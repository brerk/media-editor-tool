# How to RUN

- Modify the `docker-compose.yml`
- Run the container with `podman-compose up -d`

Or run using uv:

    `uv run server.py --api-url API_URL`

# Build

 podman build -t media-uploader .
